import { styled } from "@mui/material";

export const StyledLink = styled("a")({
  position: "relative",
  zIndex: "120",
  backgroundColor: "#fff",
  padding: "0.5em",
  borderRadius: "5px",
  color: "#000",
});
