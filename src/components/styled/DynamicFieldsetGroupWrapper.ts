import { styled } from "@mui/system";

export const DynamicFieldsetGroupWrapper = styled("div")({
  display: "flex",
  justifyContent: "start",
  gap: "10rem",
  alignItems: "space-between",
  flexWrap: "wrap",
  margin: "1em 0.5em",
});
