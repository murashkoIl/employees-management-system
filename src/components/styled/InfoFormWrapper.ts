import { styled } from "@mui/system";

export const InfoFormWrapper = styled("div")({
  display: "flex",
  overflow: "auto",
  justifyContent: "space-between",
  flexWrap: "wrap",
  margin: "1em 0.5em",
});
